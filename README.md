# 未来战争选手端

规则在 `docs/任务书.md`，接口在 `docs/接口文档.md`。当前代码怎么决策、改哪个文件、测试锁了什么，看 `docs/选手端.md`。

选手程序在 `Demo/CoreGeek/`：

```text
cd Demo/CoreGeek
python main3.py <port>
python -m pytest tests/ -q
```
